{
  "name": "scheduler-channel",
  "module": "scheduler.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun install --no-summary && bun scheduler.ts & echo $! > ${CLAUDE_PLUGIN_ROOT}/scheduler.pid && wait $!"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
